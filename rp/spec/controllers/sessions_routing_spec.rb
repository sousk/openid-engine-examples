require File.dirname(__FILE__) + '/../spec_helper'

describe AccountsController do
  describe "route generation" do
    # it "should map { :controller => 'sessions', :action => 'create' } to /session/openid" do
    #   route_for(:controller => "sessions", :action => "create").should == "/session/openid"
    # end
  end
  
  describe "route recognition" do
    # it "should generate params { :controller => 'sessions', action => 'create' } from GET /session/openid" do
    #   params_from(:get, "/session/openid").should == {:controller => "sessions", :action => "create"}
    # end
  end
end
